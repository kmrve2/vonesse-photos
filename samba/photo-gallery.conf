# Samba configuration for Photo Gallery share
# This allows users to add photos via SMB

[photo-gallery]
   comment = Photo Gallery
   path = /var/www/photos
   browseable = yes
   read only = no
   guest ok = no
   create mask = 0644
   directory mask = 0755
   force user = www-data
   valid users = uploader